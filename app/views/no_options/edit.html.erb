<h1><%= link_to("Admin", "#{root_path}admin") %> > <%= link_to("No-Option List", "#{root_path}admin/no_options", :class => 'back-link') %> > Edit No-Option</h1>

<%= form_for(:no_option, :url => { :action => 'update', :id => @no_option.id }) do |f| %>
	
	<p>Word: <%= f.text_field(:no_option) %></p>
	
	<%= submit_tag("Update No-Option")%>

<% end %>