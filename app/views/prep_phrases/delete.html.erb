<h1><%= link_to("Admin", "#{root_path}admin") %> > <%= link_to("Phrase List", "#{root_path}admin/prep_phrases", :class => 'back-link') %> > Delete Phrase</h1>

<%= form_for(:prep_phrase, :url => { :action => 'destroy', :id => @prep_phrase.id }) do |f| %>
	
	<p>Are you sure you want to delete the phrase "<%= @prep_phrase.prep_phrase %>"</p>
	
	<%= submit_tag("Delete Phrase")%>

<% end %>