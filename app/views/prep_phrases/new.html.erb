<h1><%= link_to("Admin", "#{root_path}admin") %> > <%= link_to("Phrase List", "#{root_path}admin/prep_phrases", :class => 'back-link') %> > New Phrase</h1>

<%= form_for(:prep_phrase, :url => {:action => 'create'}) do |f| %>
	
	<p>Word: <%= f.text_field(:prep_phrase) %></p>
	
	<%= submit_tag("Create New Noun")%>

<% end %>